#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
LL n, m, x[100005], y[100005];
int main() {
    cin >> n >> m;
    for(int i = 0; i <= m; ++i) {
        cin >> x[i] >> y[i];
    }
    
}