#include <bits/stdc++.h>
using namespace std;
int n, a[3000], q, x, y;
int main(){
    scanf("%d", &n);
    for(int i = 1; i <= n; ++i){
        scanf("%d", &a[i]);
    }
    scanf("%d", &q);
    while(q--) {
        scanf("%d%d", &x, &y);
    }
}
